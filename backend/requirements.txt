# Runtime dependencies for the foundation.
# RAG/LLM/DB dependencies (openai, faiss-cpu, numpy, tiktoken, sqlalchemy)
# are added in their respective sprints to keep this image small and the
# build fast. Pinned to compatible minor releases for reproducibility.
fastapi~=0.115.0
uvicorn[standard]~=0.30.0
pydantic~=2.9
pydantic-settings~=2.5
SQLAlchemy~=2.0
