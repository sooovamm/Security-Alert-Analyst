# Runtime dependencies. Pinned to compatible minor releases for reproducible
# builds, and kept current: a stale pin is a security decision, not a neutral
# one. `fastapi~=0.115.0` resolved to starlette 0.46.2, which carries 14 known
# advisories (see docs/security.md, dependency review).
#
# Re-check with:  python -m pip_audit -r requirements.txt
fastapi~=0.141.0
# Explicit floor: FastAPI itself only requires >=0.46, which resolves to a
# release with known advisories. Pin the fixed line directly.
starlette>=1.3.1
uvicorn[standard]~=0.40.0
pydantic~=2.11
pydantic-settings~=2.12
SQLAlchemy~=2.0.49
numpy~=2.2
faiss-cpu~=1.15
openai~=1.109
