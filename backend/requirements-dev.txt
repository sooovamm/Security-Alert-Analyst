# Test and lint tooling (not installed in the production image).
-r requirements.txt
pytest~=9.1
httpx~=0.28
ruff~=0.16
mypy~=1.11
# Dependency vulnerability scanning: python -m pip_audit -r requirements.txt
pip-audit~=2.10
